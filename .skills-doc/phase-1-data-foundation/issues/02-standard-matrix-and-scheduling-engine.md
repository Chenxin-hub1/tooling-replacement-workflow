# 02: 标准 6 阶段动作矩阵与调度计算引擎 (Standard Matrix & Scheduling Engine)

**What to build:**
内置 6 大阶段、31 个作为开发基线的标准动作矩阵（原始业务矩阵待核对），实现项目动作网络自动派生算法、前置依赖与交期（Lead time）驱动的到期日拓扑调度计算、红黄绿健康状态计算及团队更换时未完成动作的自动转派。

**Blocked by:** 01: 后端工程骨架、数据库模型与持久化层 (Backend Scaffold & DB Schema)

**Status:** closed
**Type:** task

- [x] 完整固化 31 个标准动作的矩阵定义（含 ph 阶段、tab 分组、act 操作、input 格式、fn 角色、lead 默认交期及 dep 前置依赖索引）。
- [x] 实现动作网络派生：给定项目和团队分配，自动生成具体的 `ProjectAction` 实例并绑定负责人。
- [x] 实现拓扑排期算法：根据基准日（立项日或前置完成日/到期日）及交期天数精准推导动作目标到期日 `due_date`，支持前置动作完成后的级联连锁更新。
- [x] 实现健康度评估算法：根据当前日期与到期日精准标记 `green`（已完成）、`red`（逾期）、`yellow`（临近到期 <=5 天）、`gray`（正常未开始）。
- [x] 实现跨职能团队人员变更时的未完成动作批量改派逻辑。
- [x] 编写核心算法的单元测试，覆盖排期、级联更新与转派核心路径及已发现的边界；不宣称穷尽覆盖。


## 实现记录（2026-09-15）

已实现并运行回归与 API 集成验证，详见 `backend/README.md` 和根目录交接记录。当前尚无 Git 提交，因此没有可引用的提交哈希；状态保留待验收，不自动关闭。

2026-09-15 用户确认继续完成整个项目，Phase-1 已验收。
