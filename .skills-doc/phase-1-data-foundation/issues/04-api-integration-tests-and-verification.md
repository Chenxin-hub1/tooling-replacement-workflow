# 04: Phase-1 核心业务链路自动化测试套件 (API Integration Tests & Verification)

**What to build:**
编写完整的 `pytest` 自动化测试套件，通过 HTTP 客户端（`httpx`）真实调用 FastAPI 接口，对 Phase-1 核心业务流转链路与关键错误路径进行集成测试，并确保 Phase-1 达成验收准则。

**Blocked by:** 03: 项目管理与工作流 REST API 接口 (REST API Endpoints)

**Status:** closed
**Type:** task

- [x] 配置 pytest 测试夹具（异步内存/临时 SQLite 数据库，测试客户端）。
- [x] 覆盖“创建项目 -> 自动派生 31 动作 -> 初始排期”链路测试。
- [x] 覆盖“修改前置动作完成状态 -> 触发级联重算后置到期日”链路测试。
- [x] 覆盖“调整团队成员 -> 未完成动作转派给新成员，已完成动作保留原成员”链路测试。
- [x] 覆盖“增加自定义动作 -> 修改动作交期 -> 状态红黄绿计算”链路测试。
- [x] 运行 `pytest` 全部通过，无告警与异常。


## 实现记录（2026-09-15）

已实现并运行回归与 API 集成验证，详见 `backend/README.md` 和根目录交接记录。当前尚无 Git 提交，因此没有可引用的提交哈希；状态保留待验收，不自动关闭。

2026-09-15 用户确认继续完成整个项目，Phase-1 已验收。
