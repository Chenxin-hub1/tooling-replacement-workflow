# v1 实施记录：按指定样板发布

2026-09-15 用户要求替换此前独立 UI，严格按指定 HTML 实现；当前先完成本机，暂不上传 Git。

| 工作 | 实施情况 | 验证 |
| --- | --- | --- |
| 固定样板 | 原文件复制到 `.design/reference/`，记录 SHA-256 | 对比原文件字节 |
| 页面入口 | 从原样板生成，原 CSS/DOM/交互函数保留 | 五个导航页、详情、弹窗、向导、手机截图 |
| 服务器保存 | 完整工作区 JSON + revision，串行事务 | 跨浏览器读取、并发冲突、保存失败 |
| 管理与动作 | 沿用样板人员/职能/规则/动作/模板 | 备注、完成、追加模板持久化 |
| Excel/工作区文件 | 原 SheetJS 格式与 JSON 备份 | 导入导出、恢复再刷新 |
| 本机交付 | 单服务静态托管、迁移 0002、启动脚本 | 实际 HTTP/浏览器验证 |
| Git/服务器 | 暂不上传；以后由用户手动部署 | 尚未发布远端 |

基准保留 31 项。Today 固定日期、邮件/Teams 预览、身份选择等原型边界原样保留，不擅自扩展。阶段当前最终状态以路线图和交接记录为准。

真实文件已提供；46 条记录、41 列，只读核对，不导入。真实列映射、占位状态、混合日期和职责映射未完成前，Phase-4 保持 in-progress。核对文档：`docs/real-data-readiness.md`。

## 已授权优化交付

实时日期、状态/排期校验、导入预览与来源保留、消息预览文案、幂等保存与本地备份、移动端已实现。78 项后端、16 项新规则单元测试和 11 条浏览器流程通过（另 4 项历史单元测试保留）。用户明确本轮不需要账号管理，通知只预览，真实文件歧义待核对，Git 不上传。详见 `docs/optimization-2026-09-15.md`。
