# 真实数据接入前核对（只读）

用户指定：`c:/Users/陈鑫/OneDrive/桌面/Master Changes and OIL 2026 - Test Tool.xlsx`。

原文件始终**未修改、未复制到仓库或上传 Git**（导入时浏览器直接从 Windows 路径读取）。2026-09-18 已按领导指示执行正式导入，见下方业务确认记录；2026-09-23 用户拍板删除全部 6 个样板演示项目，当前工作区仅剩 46 条真实项目。

## 已确认

文件存在；工作表 `Sheet1`，第一行为表头，41 列、46 条含 Part No 的数据行。本次仅用 openpyxl read_only 读取结构和内容类型，没有调用导入 API。

## 导入前必须解决

| 问题 | 当前样板行为 | 后续要求 |
| --- | --- | --- |
| 占位/未完成值 | 非空值直接标记动作完成 | 区分 PEND、TBD、NA、Completed、实际日期与自由文本，不可把待办标绿 |
| 混合日期 | 对整格调用 Date 解析 | 拆分 Completed 加日期、多行日期、说明文本，并保留原文；不猜实际完成日 |
| 主数据列名 | 只识别样板 ALIASES | 核对 Savings Anual Estimated、ZF Tool PO、Coverage、TIER 2 SOP、OEM - Tech 等映射 |
| 项目标识/日期 | 缺少 ID 就自动编号，缺日期就用固定 Today | 真实表没有样板 Project/Created 列；明确稳定项目键和日期来源，防止重复导入及错误排期 |
| 团队与业务字段 | 缺成员时用当前选人作为 Initiator | 表中 SDE/PM/Engineer 不代表所有必填职责；不得把演示人员自动分配给真实项目 |
| BPW/CVS CR | 样板有多条同名动作/不同阶段 | 明确 BPW 对应 Core 或 OEM，CVS CR 对应适用性或编号；不能一列填入多条动作 |
| 状态与追踪信息 | 导出布局不同，部分列会被忽略 | 保留 Priority、重复的 Status/STATUS、Action、Date、Responsible next action、Comment、TOOL ID、NPI 等信息并核对用途 |

## 接入顺序

先完成不改变 UI 的字段/状态映射规则与只读预检，对歧义由业务确认；使用人工构造的脱敏测试验证。全部功能与映射完成后，备份本机工作区，再按用户授权导入真实文件并逐项核对记录数、动作状态和责任人。未满足前置条件不执行导入。

## 业务确认记录

- 2026-09-18（领导答复）：无创建日期的记录统一以**导入当日**为创建日期。操作方式：导入预览的 "Confirmed creation date" 框填当天日期后 Recheck。数值上等同原样板的缺省 Today 行为，区别是经人工确认并留有预览记录。已接受的取舍：带历史完成记录的项目会出现"完成早于创建"的时间线倒挂；实际完成日期仍如实保存（原文与 Imported data review 可回溯），仅排期基线从导入日起算。
- 2026-09-18（领导指示）：**先导入、部署到服务器，问题后续迭代**。当日已执行：46 条全部新建、0 条拦截，399 条灰色提示随项目保留（`importWarnings`/`importSource`）；创建日期统一 2026-09-18；当时有 198 个动作被标为完成（包含旧规则自动完成，非全部有明确完成证据）；SDE/PM/ENG 已入团队，BU Buyer（发起人）全缺、SP/BU 大多缺，负责人留空待补。导入前备份：`.demo/before-real-import-2026-09-18.db`。BPW / CVS CR / OEM - Tech 等歧义映射仍待业务确认，确认后重导同文件即按身份更新补入。

## 优化后状态

上述表格记录的是原样板风险。现在已增加预览、缺失日期/重复身份拦截、占位值保护、明确列映射和原始数据保留。用户同意其余歧义继续待核对，未授权猜测；原始真实文件已于 2026-09-18 导入，歧义列保留待核对。当前规则见 `optimization-2026-09-15.md`。

## 2026-09-22 按新规则重导

用户明确要求重导历史数据后，已从完整 importSource 原始行重新执行当前规则并写入本地数据库：46 项目，198 项完成重新判断为 5 项，193 项重新开放。资料值和所有原始来源保留，歧义列未猜测映射。详见 [重导记录](data-reimport-2026-09-22.md)。

## 2026-09-23 歧义列映射落地

用户拍板不再等待 Q1/Q3/Q4 答复，直接采用数据分析推测的关系并重导：BPW 列按文字标签拆分 Core/OEM（无标签默认 Core 并逐行留痕警告）、CVS CR 列填入 Development 阶段编号动作、OEM - Tech 落主数据 `tech` 字段；有效编号导入即置"已发起/已创建"状态（不算完成）。46/46 处理、0 阻断，revision 3 → 4。剩余待确认仅 BPW 英文全称与 6 行 External 无标签编号的 Core 假设。详见 [映射落地记录](bpw-mapping-2026-09-23.md)。

同日用户拍板删除 6 个样板演示项目（TR-2026-014/019/021/023/027、TR-2025-088），经页面删除通道执行并落库（revision 4 → 5，删除前备份 `.demo/before-delete-demo-projects-2026-09-23.db`）；Admin 的重置演示数据仍可找回样板数据，但会覆盖当前工作区，慎用。人员与职能清单仍为样板自带名单（当前不启用登录，身份切换沿用原型选人）。
