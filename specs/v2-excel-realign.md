# v2 Phase-5 — Excel 字段对齐

首表保留原动作列，新增动作级 Approval status、Original date、Current date、Completed date，动作明细增加审批状态、双日期和原始日期修正记录。导入识别这些扩展列；显式中间状态不完成动作，明确实际完成日期才回填完成时间。

- 空值与 PEND/TBD 等占位值继续保护既有确认值；歧义内容留在 importSource / importWarnings。
- 已有原始日期与导入文件不一致时阻止应用，提示通过 Admin 修正并留痕；新项目可从导出列恢复原始日期。
- 首表可回导状态、双日期和完成日期；完整交期、修正历史、模板、人员与退役阶段仍以工作区 JSON 恢复。
- SheetJS 从 0.18.5 升级至官方 0.20.3，修复已公布的原型污染及正则拒绝服务漏洞；解析行列范围在转换二维数组前检查。
- JSON 工作区文件须通过只读 `/api/workspace/validate` 再替换内存，失败不影响当前工作区。

代码与往返测试已完成；用户已要求历史数据重新按新规则导入，取消完成状态豁免。真实文件的 BPW / CVS CR / OEM - Tech 歧义映射原等待 Q1/Q3/Q4；2026-09-23 用户拍板按推测关系落地并重导（见 `docs/bpw-mapping-2026-09-23.md`），本阶段转 awaiting-acceptance。

本次补齐：六处 dateLog 历史支持 JSON/Excel 往返，已有项目导入修改追加 import 事件，无变化不重复追加。普通值与适用性导入不完成；有效完成日期和最终批准作为完成证据，修改凭证重新开放。CVS CR 数据兼容迁至 Development。

重导规则：只重新判断本次文件涉及的动作；普通值没有实际完成日期保持未完成，审批动作没有本次明确最终批准重新开放。合法的显式完成日期保留。原值与最终批准均未变化时，可保留真实的已有完成日期，不能把重导当天当作历史批准日。未涉及的项目、自定义动作、人员及日期历史保持原样。
