# v2 Phase-3 — 四阶段流程

2026-09-22 按已确认的 B9 删除 Scrap 和 Archive。以代码矩阵逐项核对：原 31 项中两个阶段为 5 + 6 = 11 项，保留 20 项标准动作（立项无标准动作，后三阶段分别 9、7、4 项）。修正提案中“删除 10 项、剩 21 项”的计数错误。

- 项目进度条、时间线、向导、阶段筛选、待办、管理统计和导出统一为四阶段；Accounting 保留人员角色，标准模板不再分配其任务。
- 旧工作区加载时，将第 5/6 阶段动作移入各项目 `retiredActions`，保留全部原字段；当前页面与 Excel 排除这些动作，工作区 JSON 仍可查阅。
- 保留阶段中的自定义动作不删除；引用旧阶段的依赖转为立项基准，原引用记在 `retiredDependency`。模板索引依赖随删除重新映射。
- 后端新建项目使用 20 项模板，汇总返回四阶段。历史规范化表不自动删除；它们不参与当前工作区页面的数据流。
- 验证：规则层覆盖无损迁移和模板依赖重排；浏览器覆盖四段进度、向导、视图和导出；真实数据库副本升级与工作区转换校验。
